{
  "name": "rest-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "start": "NODE_ENV=development netlify-lambda serve src --openssl-legacy-provider",
  "build": "NODE_ENV=production netlify-lambda build src",
  "start-production": "NODE_OPTIONS='--openssl-legacy-provider' node src/server.js"
}

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0"
  }
}
